services:
  db:
    image: postgres:15-alpine
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - pg-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  redis:
    image: redis:7-alpine
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data

  backend:
    build:
      context: ./backend
      dockerfile: ../docker/backend/Dockerfile
    restart: always
    command: /app/entrypoint.sh
    environment:
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      BORG_PASSPHRASE: ${BORG_PASSPHRASE}
      ORCHESTRATOR_IP: ${ORCHESTRATOR_IP}
      BORG_HOST_DATA_PATH: ${BORG_HOST_DATA_PATH}
      PYTHONPATH: /app
      SUPERADMIN_USERNAME: ${SUPERADMIN_USERNAME}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
    volumes:
      - ./backend:/app
      - ${BORG_HOST_DATA_PATH:-borg-data}:/data/borg
      - ssh-keys:/root/.ssh
      - iso-cache:/opt/data/iso_cache
      - /proc:/host/proc:ro
      - ./payload_client:/payload_client
      - frontend-build:/opt/frontend_build
    ports:
      - "8000:8000"
    depends_on:
      - db
      - redis
    dns:
      - 8.8.8.8
      - 10.102.1.21

  worker:
    build:
      context: ./backend
      dockerfile: ../docker/backend/Dockerfile
    restart: always
    privileged: true
    command: celery -A tasks.celery_app worker --loglevel=info
    environment:
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      BORG_PASSPHRASE: ${BORG_PASSPHRASE}
      ORCHESTRATOR_IP: ${ORCHESTRATOR_IP}
      PYTHONPATH: /app
      SUPERADMIN_USERNAME: ${SUPERADMIN_USERNAME}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
    volumes:
      - ./backend:/app
      - /dev:/dev
      - ${BORG_HOST_DATA_PATH:-borg-data}:/data/borg
      - ssh-keys:/root/.ssh
      - iso-cache:/opt/data/iso_cache
      - frontend-build:/opt/frontend_build
      - ./payload_client:/payload_client
      - /proc:/host/proc:ro
      - /var/run/docker.sock:/var/run/docker.sock
    depends_on:
      - db
      - redis
    dns:
      - 8.8.8.8
      - 10.102.1.21

  beat:
    build:
      context: ./backend
      dockerfile: ../docker/backend/Dockerfile
    restart: always
    command: celery -A tasks.celery_app beat --loglevel=info
    environment:
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      BORG_PASSPHRASE: ${BORG_PASSPHRASE}
      ORCHESTRATOR_IP: ${ORCHESTRATOR_IP}
      PYTHONPATH: /app
      SUPERADMIN_USERNAME: ${SUPERADMIN_USERNAME}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
    volumes:
      - ./backend:/app
      - ${BORG_HOST_DATA_PATH:-borg-data}:/data/borg
      - ssh-keys:/root/.ssh
    depends_on:
      - db
      - redis

  borg-server:
    build:
      context: .
      dockerfile: docker/borg/Dockerfile
    restart: always
    ports:
      - "12345:22"
    volumes:
      - ${BORG_HOST_DATA_PATH:-borg-data}:/data/borg
      - ssh-keys:/home/borg/.ssh
    depends_on:
      - db

  frontend:
    build:
      context: ./frontend
      dockerfile: ../docker/frontend/Dockerfile
    restart: always
    ports:
      - "7777:7777"
    volumes:
      - frontend-build:/usr/share/nginx/html
    depends_on:
      - backend

  apt-proxy:
    build:
      context: .
      dockerfile: docker/apt-proxy/Dockerfile
    restart: always
    ports:
      - "3142:3142"
    volumes:
      - apt-cache:/var/cache/apt-cacher-ng
    dns:
      - 8.8.8.8
      - 10.102.1.21

volumes:
  pg-data:
    name: backup-edge-restore_pg-data
    external: true
  borg-data:
    name: backup-edge-restore_borg-data
    external: true
  ssh-keys:
    name: backup-edge-restore_ssh-keys
    external: true
  redis-data:
    name: backup-edge-restore_redis-data
    external: true
  iso-cache:
    name: backup-edge-restore_iso-cache
    external: true
  frontend-build:
    name: backup-edge-restore_frontend-build
    external: true
  apt-cache:
    name: backup-edge-restore_apt-cache
    external: true

